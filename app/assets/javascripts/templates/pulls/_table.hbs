<div class='row'>
  <div class='pulls-container'>
    {{#if isLoading}}
      Loading...
    {{else}}
      <table id='pulls-table'>
        <thead>
          <th class='hidden-xs'>Author</th>
          <th>Title</th>
          <th class='hidden-xs'>Repo</th>
          <th><a class='created-at-sort' href='#' {{action 'sort'}}>Created At</a></th>
          <th>Actions</th>
        </thead>
        <tbody>
          {{#each}}
          <tr>
              <td class='user-avatar'>
                <a {{bind-attr href='user.url'}} target='_blank'>
                  <img {{bind-attr src='user.avatar' alt='user.nickname'}} />
                </a>
              </td>
              <td>
                <a {{bind-attr href='url'}} target='_blank' onClick='ga("send", "event", "pull_link", "click")'>{{title}}</a>
              </td>
              <td class='hidden-xs'>
                <a {{bind-attr href='repository.url'}} target='_blank' onClick='ga("send", "event", "repo_link", "click")'>{{repository.full_name}}</a>
              </td>
              <td>
                {{friendlyDate created_at}}
              </td>
              <td>
                <button class='show-diff' {{action 'showDiff' this}}><span></span> Review</button>
              </td>
            </tr>
          {{else}}
            <tr>
              <td class='center' colspan='5'>No Pull Requests to show.</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    {{/if}}
  </div>
</div>

